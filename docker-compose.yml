name: yrvi

x-python-env: &python-env
  PYTHONUNBUFFERED: "1"
  PYTHONDONTWRITEBYTECODE: "1"
  YRVI_CONTAINERIZED: "1"
  YRVI_DATA_DIR: /data
  YRVI_INIT_DRY_RUN: ${YRVI_INIT_DRY_RUN:-true}
  TRADING_MODE: ${TRADING_MODE:-paper}
  IBKR_HOST: ib_gateway
  # gnzsnz/ib-gateway exposes paper on 4004 and live on 4003 inside the Docker network.
  IBKR_PORT: ${IBKR_PORT:-4004}
  IBKR_CLIENT_ID: ${IBKR_CLIENT_ID:-1}
  RENDER_URL: ${RENDER_URL:-https://yourockclub-ledger-sync.onrender.com/api/targets/csp}

x-python-secrets: &python-secrets
  - render_secret
  - anthropic_api_key
  - discord_webhook_url
  - discord_webhook_weekly_plan
  - ibkr_password_live

services:
  secrets:
    build: ./docker/secrets_service
    container_name: yrvi-secrets-1
    ports:
      - "8001:8001"
    volumes:
      - yrvi_data:/data
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8001/health', timeout=5)"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  ib_gateway:
    build:
      context: ./docker/ib-gateway
      args:
        IB_GATEWAY_TAG: ${IB_GATEWAY_TAG:-latest}
    container_name: ib_gateway
    restart: "no"
    entrypoint: ["/usr/local/bin/yrvi-gw-entrypoint.sh"]
    command: ["/home/ibgateway/scripts/run.sh"]
    depends_on:
      secrets:
        condition: service_healthy
    environment:
      # TWS_USERID and VNC_SERVER_PASSWORD are fetched from the secrets container
      # at startup by yrvi-gw-entrypoint.sh — not injected here.
      TRADING_MODE: ${TRADING_MODE:-paper}
      TWS_ACCEPT_INCOMING: ${TWS_ACCEPT_INCOMING:-accept}
      READ_ONLY_API: ${READ_ONLY_API:-no}
      TIME_ZONE: ${TIME_ZONE:-America/Los_Angeles}
      TZ: ${TIME_ZONE:-America/Los_Angeles}
      TWOFA_TIMEOUT_ACTION: ${TWOFA_TIMEOUT_ACTION:-exit}
      RELOGIN_AFTER_TWOFA_TIMEOUT: ${RELOGIN_AFTER_TWOFA_TIMEOUT:-no}
      TWOFA_EXIT_INTERVAL: ${TWOFA_EXIT_INTERVAL:-60}
      EXISTING_SESSION_DETECTED_ACTION: ${EXISTING_SESSION_DETECTED_ACTION:-primary}
      ALLOW_BLIND_TRADING: ${ALLOW_BLIND_TRADING:-no}
      AUTO_RESTART_TIME: ${AUTO_RESTART_TIME:-11:59 PM}
      TWS_SETTINGS_PATH: /home/ibgateway/Jts
    volumes:
      - ib_gateway_settings:/home/ibgateway/Jts
    ports:
      - "127.0.0.1:${IB_GATEWAY_LIVE_PORT:-4001}:4003"
      - "127.0.0.1:${IB_GATEWAY_PAPER_PORT:-4002}:4004"
      - "127.0.0.1:${IB_GATEWAY_VNC_PORT:-5900}:5900"

  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    image: yrvi-api:local
    restart: unless-stopped
    depends_on:
      ib_gateway:
        condition: service_started
      secrets:
        condition: service_healthy
    environment:
      <<: *python-env
    secrets: *python-secrets
    volumes:
      - yrvi_data:/data
    ports:
      - "127.0.0.1:${YRVI_API_PORT:-8000}:8000"
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  scheduler:
    build:
      context: .
      dockerfile: Dockerfile.scheduler
    image: yrvi-scheduler:local
    restart: unless-stopped
    depends_on:
      ib_gateway:
        condition: service_started
      secrets:
        condition: service_healthy
    environment:
      <<: *python-env
    secrets: *python-secrets
    volumes:
      - yrvi_data:/data
    healthcheck:
      test: ["CMD", "python3", "/app/docker/healthcheck-scheduler.py"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 120s

  web:
    build:
      context: .
      dockerfile: yrvi-app/Dockerfile
    image: yrvi-web:local
    restart: unless-stopped
    depends_on:
      - api
    ports:
      - "127.0.0.1:${YRVI_WEB_PORT:-3000}:80"

secrets:
  tws_password_paper:
    file: ${YRVI_SECRETS_DIR:-./docker/secrets}/tws_password_paper
  tws_password_live:
    file: ${YRVI_SECRETS_DIR:-./docker/secrets}/tws_password_live
  render_secret:
    file: ${YRVI_SECRETS_DIR:-./docker/secrets}/render_secret
  anthropic_api_key:
    file: ${YRVI_SECRETS_DIR:-./docker/secrets}/anthropic_api_key
  discord_webhook_url:
    file: ${YRVI_SECRETS_DIR:-./docker/secrets}/discord_webhook_url
  discord_webhook_weekly_plan:
    file: ${YRVI_SECRETS_DIR:-./docker/secrets}/discord_webhook_weekly_plan
  ibkr_password_live:
    file: ${YRVI_SECRETS_DIR:-./docker/secrets}/ibkr_password_live

volumes:
  ib_gateway_settings:
  yrvi_data:
